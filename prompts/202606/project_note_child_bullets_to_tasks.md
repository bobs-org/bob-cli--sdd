---
plan: sdd/tales/202606/project_note_child_bullets_to_tasks.md
---
 When we use the `<ctrl+alt+shift+n>` keymap today to promote an Obsidian task to a project note file, we currently abort the promotion if the target tasks has any sub-bullets. Can you help me instead start converting those sub-bullets to Obsidian tasks in the "Tasks" section of the new project note file (in which case, we should not render--or delete after rendering--the default Obsidian task with the placeholder text that normally goes in that section).

Think this through thoroughly and create a plan using your `/sase_plan` skill. Submit your plan with the
`sase plan propose` command (as the skill instructs) before making any file changes.
