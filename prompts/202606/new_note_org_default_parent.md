---
plan: sdd/tales/202606/new_note_org_default_parent.md
---
 #fork:3h This works, but can you make it so that the `parent` property defaults to using `[[org]]` for its value in that case? Think this through thoroughly and create a plan using your `/sase_plan` skill. Submit your plan with the `sase plan`
command (as the skill instructs) before making any file changes.
