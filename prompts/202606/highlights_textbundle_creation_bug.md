---
plan: sdd/tales/202606/highlights_textbundle_creation_bug.md
---
 Can you help me document a current bug in the Highlights app that affects the `bob highlight scan` command? Namely the user needs to export a text bundle directory in the appropriate location when a PDF document has image annotations. The highlights app updates this text bundle correctly but fails to create it, which I think is a bug in that app. Think this through thoroughly and create a plan using your `/sase_plan` skill. Submit your plan with the
`sase plan propose` command (as the skill instructs) before making any file changes.
 