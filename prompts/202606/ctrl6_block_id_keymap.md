---
plan: sdd/tales/202606/ctrl6_block_id_keymap.md
---
 #fork:60  Can you now help me add a new `<ctrl+6>` keymap that allows me to rename (or add a block ID/name, if one does not already exist) the block that is currently selected? This should also work on transcluded block links on the current line (if found, prioritize renaming the trancluded block link instead of renaming the block that contains the transcluded block link).

Think this through thoroughly and create a plan using your `/sase_plan` skill. Submit your plan with the `sase plan`
command (as the skill instructs) before making any file changes.
