---
plan: sdd/tales/202606/formalize_type_property.md
---
 Can you help me formalize the `type` frontmatter property that is used on some of my Obsidian note files?

- All of these properties should have a note file link (ex: `[[foobar]]`) as its value.
- All daily, monthly, and yearly files should have `[[day]]` as their `type` property value.
- Create new note files to represent types that are needed but that do not yet have corresponding note files. Give them a good description as their contents and link back to the `[[type]]` note via the `parent` property.
- Make sure to add Metadata Menu support for the `type` property (so I see a useful dropdown when entering the value for this property in Obsidian).

Think this through thoroughly and create a plan using your `/sase_plan` skill. Submit your plan with the `sase plan`
command (as the skill instructs) before making any file changes.
