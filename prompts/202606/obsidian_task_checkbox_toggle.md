---
plan: sdd/tales/202606/obsidian_task_checkbox_toggle.md
---
 Can you help me add a new `<ctrl+]>` keymap to my Obsidian vault (~/bob) that toggles whether a bullet is a checkmark or not? For example, using it for the `  - [ ] Foo bar baz` line would convert the line to `  - Foo bar baz` (pressing `<ctrl+]>` again would convert the line back). Think this through thoroughly and create a plan using your `/sase_plan` skill. Submit your plan with the `sase plan`
command (as the skill instructs) before making any file changes.
