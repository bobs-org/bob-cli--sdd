---
plan: sdd/tales/202606/vim_backspace_link_action.md
---
 #fork:2q.f1  Can you now help me add a new vim-mode normal mode `<backspace>` keymap that works in a similar way, but counts backwards? The default target line for this keymap should be the line before the current line (e.g. `<backspace>` has the same effect as `1<backspace>`). Think this through thoroughly and create a plan using your `/sase_plan` skill. Submit your plan with the `sase plan`
command (as the skill instructs) before making any file changes.
