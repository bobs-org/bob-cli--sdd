---
plan: sdd/tales/202606/obsidian_file_link_caret_jump.md
---
 #fork:25 Actually, can we just delete `|foobar`? Also, pressing `^` after the last `]` in a file link (block links should trigger the block rename functionality) should always result in the cursor moving 2 to the left (after deleting `|foobar` in the case of a link alias). Think this through thoroughly and create a plan using your `/sase_plan` skill. Submit your plan with the `sase plan`
command (as the skill instructs) before making any file changes.
