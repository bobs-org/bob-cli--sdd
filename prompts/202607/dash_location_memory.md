---
plan: .sase/sdd/tales/202607/dash_location_memory.md
---
 The `<ctrl+0>` keymap in Obsidian currently allows the user to easily jump to the ~/bob/dash.md file, but that file is always scrolled to the top after opened, even if it was already open in another tab and the user was looking at a specific part of the file (e.g. near the middle or bottom). Can you help me start remembering the last line location for this file and jumping to that location? IMPORTANT: This file contains Obsidian Tasks queries, which can span many virtual lines, so we need to make sure that we account for that (I think the `<ctrl+d/u>` keymaps account for these somehow--maybe it would be useful to see how those do it). Think this through thoroughly and create a plan using your `/sase_plan` skill. Submit your plan with the
`sase plan propose` command (as the skill instructs) before making any file changes.
